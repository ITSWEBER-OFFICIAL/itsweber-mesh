TZ=Europe/Berlin
LOG_LEVEL=info
DATA_DIR=/data
TAG=latest

# Session-Secret — PFLICHT (≥ 32 Zeichen).
# Erzeugen: openssl rand -hex 32
# Beim Container-Replace IMMER denselben Wert wiederverwenden,
# sonst werden alle aktiven Sessions invalidiert.
MESH_SESSION_SECRET=
