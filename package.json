{
  "name": "itsweber-mesh",
  "version": "1.0.0",
  "private": true,
  "license": "AGPL-3.0",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.15.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "pnpm --filter web test",
    "test:build-safe": "pnpm --filter web test:build-safe",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^6.0.3"
  }
}
